import { ITextType } from './custom-text.types';

export const TEXT_TYPE: ITextType = {
  title: {
    defaultSize: 24,
    defaultWeight: 700,
  },
  text: {
    defaultSize: 17,
    defaultWeight: 500,
  },
};
